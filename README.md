# 🎨 Real-Time Collaborative Drawing Canvas

A multi-user whiteboard app built with **Vanilla JS + Node.js + Socket.io** that allows multiple people to draw simultaneously in real-time on a shared canvas.

---

## 🚀 Setup Instructions

1. Clone or download this repository  
2. Install dependencies:
   ```bash
   npm install
